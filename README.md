Description:
This project contains an application that fetches country data from a JSON API and displays relevant information.

Features:

Fetches country data dynamically

Displays country details (name, population, capital, etc.)

Interactive UI

Usage:

Clone the project:

git clone https://github.com/TeomanHasgoren/JSON-Country-APP.git

Navigate to the project directory and open index.html in a browser.
